services:
  photoapp:
    image: 555cijaiz555/reactphoto:v10
    container_name: reactphotoappv10
    ports:
      - "3001:3001"
    volumes:
      - /home/ubuntu/photoapp/uploads:/app/uploads
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped
    networks:
      - photonet

networks:
  photonet:
    driver: bridge

volumes:
  uploads:
    driver: local