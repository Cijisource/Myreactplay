name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            reactdb/backend/package-lock.json
            reactdb/frontend/package-lock.json

      - name: Install backend dependencies
        working-directory: reactdb/backend
        run: npm ci

      - name: Build backend
        working-directory: reactdb/backend
        run: npm run build

      - name: Install frontend dependencies
        working-directory: reactdb/frontend
        run: npm ci

      - name: Build frontend
        working-directory: reactdb/frontend
        run: npm run build

      - name: Build Docker images
        run: |
          docker build -t reactdb-backend:test reactdb/backend
          docker build -t reactdb-frontend:test reactdb/frontend
